function getUserIpDetails(userResponse = {}) {
    const { userName, userIp = '0.0.0.0' } = userRespone;
    return [userName, userIp];
}

function getTopThree(studentMarks = []) {
    const[first=null, second]
}